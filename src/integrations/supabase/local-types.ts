/**
 * local-types.ts
 * Tipos locais usados na integração com React Router e estado da aplicação.
 * Separados do types.ts gerado automaticamente pelo Supabase.
 */

export interface ReaderLocationState {
  fromRevela?: boolean;
  book?: string;
  chapter?: number;
  verse?: number;
}
